// Generated by CoffeeScript 1.6.2
(function() {
  var andp, orp,
    __slice = [].slice;

  exports.solver = {};

  solver.failcont = function(v) {
    return v;
  };

  orp = function() {
    var args;

    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    return function(cont) {
      var fc;

      switch (args.length) {
        case 1:
          return args[0](cont);
        case 2:
          fc = solver.failcont;
          solver.failcont = function(v) {
            var failcont;

            failcont = fc;
            return args[1](cont);
          };
          return args[0](cont);
        default:
          fc = failcont;
          solver.failcont = function(v) {
            solver.failcont = fc;
            return orp.apply(null, args.slice(1))(cont);
          };
          return args[0](cont);
      }
    };
  };

  andp = function() {
    var args;

    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    return function(cont) {
      switch (args.length) {
        case 1:
          return args[0](cont);
        case 2:
          return args[0](args[1](cont));
        default:
          return args[0](andp.apply(null, args.slice(1))(cont));
      }
    };
  };

}).call(this);

/*
//@ sourceMappingURL=globalstatesolver.map
*/
