// Generated by CoffeeScript 1.6.2
(function() {
  var compile, core, coreSolve, dao, solve, string, util, xexports, _ref;

  string = (util = require('../lib/util')).string;

  coreSolve = (core = require('../lib/core')).coreSolve;

  _ref = dao = require('../lib/dao'), solve = _ref.solve, compile = _ref.compile;

  coreSolve = function(v) {
    return v;
  };

  xexports = {};

  exports.Test = {
    "test atomic": function(test) {
      test.equal(solve('3'), 3);
      test.equal(solve('123'), 123);
      test.deepEqual(solve('-123.56e-3'), coreSolve([128, 0.12356]));
      test.deepEqual(solve('"1"'), coreSolve(string('"1"')));
      return test.done();
    },
    "test string": function(test) {
      test.deepEqual(solve('1+1'), coreSolve([109, 1, 1]));
      return test.done();
    }
  };

}).call(this);

/*
//@ sourceMappingURL=test_dao.map
*/
