// Generated by CoffeeScript 1.6.2
(function() {
  var ArgumentError, ArityError, Compiler, Env, Error, TypeError, augmentOperators, beautify, compile, fs, il, _, _ref, _ref1, _ref2,
    __slice = [].slice,
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  _ = require("underscore");

  fs = require("fs");

  beautify = require('js-beautify').js_beautify;

  il = require("./interlang");

  exports.solve = function(exp, path) {
    var compiled;

    path = compile(exp, path);
    delete require.cache[require.resolve(path)];
    compiled = require(path);
    return compiled.main();
  };

  compile = function(exp, path) {
    var code, compiler, fd;

    compiler = new Compiler();
    code = "_ = require('underscore');\n" + '__slice = [].slice\n' + "solve = require('f:/daonode/lib/compiler/core.js').solve;\n" + "parser = require('f:/daonode/lib/compiler/parser.js');\n" + "solvecore = require('f:/daonode/lib/compiler/solve.js');\n" + "SolverFinish = solvecore.SolverFinish;\n" + "Solver = solvecore.Solver;\n" + "Trail = solvecore.Trail;\n" + "Var = solvecore.Var;\n" + "DummyVar = solvecore.DummyVar;\n\n" + compiler.compile(exp) + "\n//exports.main();";
    code = beautify(code, {
      indent_size: 2
    });
    path = path || "f:/daonode/lib/compiler/test/compiled.js";
    fd = fs.openSync(path, 'w');
    fs.writeSync(fd, code);
    fs.closeSync(fd);
    return path;
  };

  exports.Compiler = Compiler = (function() {
    var e, instance, name, vop, _fn, _i, _len, _ref;

    function Compiler() {
      this.nameToVarIndex = {};
      this.exits = {};
      this.continues = {};
      this.protect = function(cont) {
        return cont;
      };
    }

    Compiler.prototype.compile = function(exp) {
      var f, fromCont, v;

      v = il.vari('v');
      fromCont = this.cont(exp, il.clamda(v, il["throw"](il["new"](il.symbol('SolverFinish').call(v)))));
      f = il.assign(il.vari('exports.main'), il.clamda(v, il.assign(il.solver, il["new"](il.symbol('Solver').call())), il.assign(il.state, null), il.assign(il.catches, {}), il.assign(il.trail, il.newTrail), il.assign(il.failcont, il.clamda(v, il["throw"](il["new"](il.symbol('SolverFinish').call(v))))), il.assign(il.cutcont, il.failcont), il.run(il.clamda(v, fromCont))));
      f.refMap = {};
      f.analyze(this, f.refMap);
      f = f.optimize(new Env(), this);
      f = f.jsify();
      return f.toCode(this);
    };

    Compiler.prototype.xxxanalyse = function(exp) {
      var body, bodyVars, head, length, result;

      if (_.isString(exp)) {
        return [exp];
      }
      if (!_.isArray(exp)) {
        return [];
      }
      length = exp.length;
      if (length === 0) {
        return [];
      }
      head = exp[0];
      if (!_.isString(head)) {
        return [];
      }
      if (!this.specials.hasOwnProperty(head)) {
        return [];
      }
      switch (head) {
        case 'string':
          return [];
        case 'logicvar':
          return [];
        case 'quote':
          return [];
        case 'quasiquote':
          return [];
        case 'lambda':
        case 'macro':
          bodyVars = analyseExps(exp.slice(2));
          exp.bodyVars = bodyVars;
          exp.vars = result = _.difference(bodyVars, exp[1]);
          return result;
        case 'with-vars':
          body = exp.slice(2);
          return bodyVars = analyseExps(exp.slice(2));
        default:
          return analyseExps(exp.slice(1));
      }
    };

    Compiler.prototype.cont = function(exp, cont) {
      var head, length, _ref;

      if (_.isString(exp)) {
        return cont.call(il.vari(exp));
      }
      if (!_.isArray(exp)) {
        return cont.call(exp);
      }
      length = exp.length;
      if (length === 0) {
        return cont.call(exp);
      }
      head = exp[0];
      if (!_.isString(head)) {
        return cont.call(exp);
      }
      if (!this.specials.hasOwnProperty(head)) {
        return cont.call(exp);
      }
      return (_ref = this.specials[head]).call.apply(_ref, [this, cont].concat(__slice.call(exp.slice(1))));
    };

    Compiler.prototype.leftValueCont = function(cont, task, item, exp, op) {
      var cont1, head, i, index, length, obj, object, v;

      if (_.isString(item)) {
        v = il.vari('v');
        if (task === 'assign') {
          return this.cont(exp, il.clamda(v, il.assign(il.vari(item), v), cont.call(v)));
        } else if (task === 'augment-assign') {
          return this.cont(exp, il.clamda(v, new augmentOperators[op](il.vari(item), v), cont.call(v)));
        } else {
          return cont.call(il[task](il.vari(item)));
        }
      }
      if (!_.isArray(item)) {
        throw new Error("Left Value should be an sexpression.");
      }
      length = item.length;
      if (length === 0) {
        throw new Error("Left Value side should not be empty list.");
      }
      head = item[0];
      if (!_.isString(head)) {
        throw new Error("Keyword should be a string.");
      }
      if (head === "index") {
        object = item[1];
        index = item[2];
        obj = il.vari('obj');
        i = il.vari('i');
        v = il.vari('v');
        if (task === 'assign') {
          cont1 = this.cont(exp, il.clamda(v, il.assign(il.index(obj, i), cont.call(v))));
        } else if (task === 'augment-assign') {
          cont1 = this.cont(exp, il.clamda(v, new augmentOperators[op](il.index(obj, i), cont.call(v))));
        } else {
          cont1 = cont.call(il[task](il.index(obj, i)));
        }
        return this.cont(object, il.clamda(obj, this.cont(index, il.clamda(i, cont1))));
      } else {
        throw new Error("Left Value side should be assignable expression.");
      }
    };

    Compiler.prototype.specials = {
      "quote": function(cont, exp) {
        return cont.call(exp);
      },
      "eval": function(cont, exp, path) {
        var p, v;

        v = il.vari('v');
        p = il.vari('path');
        return this.cont(exp, il.clamda(v, this.cont(path, il.clamda(p, cont.call(il.evalexpr(v, p))))));
      },
      'string': function(cont, exp) {
        return cont.call(exp);
      },
      "begin": function() {
        var cont, exps;

        cont = arguments[0], exps = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
        return this.expsCont(exps, cont);
      },
      "assign": function(cont, left, exp) {
        return this.leftValueCont(cont, "assign", left, exp);
      },
      "augment-assign": function(cont, op, left, exp) {
        return this.leftValueCont(cont, "augment-assign", left, exp, op);
      },
      'inc': function(cont, item) {
        return this.leftValueCont(cont, "inc", item);
      },
      'suffixinc': function(cont, item) {
        return this.leftValueCont(cont, "suffixinc", item);
      },
      'dec': function(item) {
        return this.leftValueCont(cont, "dec", item);
      },
      'suffixdec': function(item) {
        return this.leftValueCont(cont, "suffixdec", item);
      },
      "if": function(cont, test, then_, else_) {
        var v;

        v = il.vari('v');
        return this.cont(test, il.clamda(v, il.if_(v, this.cont(then_, cont), this.cont(else_, cont))));
      },
      "jsfun": function(cont, func) {
        var f;

        f = il.jsfun(func);
        f._effect = this._effect;
        return cont.call(f);
      },
      "pure": function(cont, exp) {
        var oldEffect, result;

        oldEffect = this._effect;
        this._effect = il.PURE;
        result = this.cont(exp, cont);
        this._effect = oldEffect;
        return result;
      },
      "effect": function(cont, exp) {
        var oldEffect, result;

        oldEffect = this._effect;
        this._effect = il.EFFECT;
        result = this.cont(exp, cont);
        this._effect = oldEffect;
        return result;
      },
      "io": function(cont, exp) {
        var oldEffect, result;

        oldEffect = this._effect;
        this._effect = il.IO;
        result = this.cont(exp, cont);
        this._effect = oldEffect;
        return result;
      },
      "lambda": function() {
        var body, cont, k, p, params;

        cont = arguments[0], params = arguments[1], body = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        k = il.vari('cont');
        params = (function() {
          var _i, _len, _results;

          _results = [];
          for (_i = 0, _len = params.length; _i < _len; _i++) {
            p = params[_i];
            _results.push(il.vari(p));
          }
          return _results;
        })();
        return cont.call(il.lamda([k].concat(params), this.expsCont(body, k)));
      },
      "macro": function() {
        var body, cont, i, k, p, params, params1;

        cont = arguments[0], params = arguments[1], body = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        k = il.vari('cont');
        params1 = (function() {
          var _i, _len, _results;

          _results = [];
          for (_i = 0, _len = params.length; _i < _len; _i++) {
            p = params[_i];
            _results.push(il.vari(p));
          }
          return _results;
        })();
        body = (function() {
          var _i, _ref, _results;

          _results = [];
          for (i = _i = 0, _ref = body.length; 0 <= _ref ? _i < _ref : _i > _ref; i = 0 <= _ref ? ++_i : --_i) {
            _results.push(this.substMacroArgs(body[i], params));
          }
          return _results;
        }).call(this);
        return cont.call(il.lamda([k].concat(params1), this.expsCont(body, k)));
      },
      'with-vars': function(cont, names, body) {
        var bindings, env;

        bindings = {};
        bindings[name] = this.newvar(name);
        env = this.withEnv;
        this.withEnv = env.extendBindings(bindings);
        cont = this.cont(['begin'].concat(__slice.call(body)));
        this.withEnv = env;
        return cont;
      },
      "evalarg": function(cont, name) {
        return cont.call(il.vari(name).call(cont));
      },
      "funcall": function() {
        var args, caller, compiler, cont, f, i, length, params, _i, _ref;

        cont = arguments[0], caller = arguments[1], args = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        compiler = this;
        f = il.vari('f');
        length = args.length;
        params = (function() {
          var _i, _results;

          _results = [];
          for (i = _i = 0; 0 <= length ? _i < length : _i > length; i = 0 <= length ? ++_i : --_i) {
            _results.push(il.vari('a' + i));
          }
          return _results;
        })();
        cont = f.apply([cont].concat(params));
        for (i = _i = _ref = length - 1; _i >= 0; i = _i += -1) {
          cont = (function(i, cont) {
            return compiler.cont(args[i], il.clamda(params[i], cont));
          })(i, cont);
        }
        return this.cont(caller, il.clamda(f, cont));
      },
      "macall": function() {
        var args, caller, compiler, cont, f, i, length, params, v, _i, _ref;

        cont = arguments[0], caller = arguments[1], args = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        compiler = this;
        f = il.vari('f');
        v = il.vari('v');
        length = args.length;
        params = (function() {
          var _i, _results;

          _results = [];
          for (i = _i = 0; 0 <= length ? _i < length : _i > length; i = 0 <= length ? ++_i : --_i) {
            _results.push(il.vari('a' + i));
          }
          return _results;
        })();
        cont = f.apply([cont].concat(params));
        for (i = _i = _ref = length - 1; _i >= 0; i = _i += -1) {
          cont = (function(i, cont) {
            return il.clamda(params[i], cont).call(il.lamda([], compiler.cont(args[i], il.idcont)));
          })(i, cont);
        }
        return this.cont(caller, il.clamda(f, cont));
      },
      "quasiquote": function(cont, exp) {
        return this.quasiquote(exp, cont);
      },
      "unquote": function(cont, exp) {
        throw new Error("unquote: too many unquote and unquoteSlice");
      },
      "unquote-slice": function(cont, exp) {
        throw new Error("unquoteSlice: too many unquote and unquoteSlice");
      },
      'block': function() {
        var body, cont, continues, defaultContinues, defaultExits, exits, f, fun, label, _base, _base1, _base2, _base3, _ref, _ref1, _ref2, _ref3;

        cont = arguments[0], label = arguments[1], body = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        label = label[1];
        if (!_.isString(label)) {
          label = '';
          body = [label].concat(body);
        }
        exits = (_ref = (_base = this.exits)[label]) != null ? _ref : _base[label] = [];
        exits.push(cont);
        defaultExits = (_ref1 = (_base1 = this.exits)['']) != null ? _ref1 : _base1[''] = [];
        defaultExits.push(cont);
        continues = (_ref2 = (_base2 = this.continues)[label]) != null ? _ref2 : _base2[label] = [];
        f = il.vari('block' + label);
        fun = il.clamda(il.vari('v'), null);
        continues.push(f);
        defaultContinues = (_ref3 = (_base3 = this.continues)['']) != null ? _ref3 : _base3[''] = [];
        defaultContinues.push(f);
        fun.body = this.expsCont(body, cont);
        exits.pop();
        if (exits.length === 0) {
          delete this.exits[label];
        }
        continues.pop();
        if (continues.length === 0) {
          delete this.continues[label];
        }
        defaultExits.pop();
        defaultContinues.pop();
        return il.begin(il.assign(f, fun), f.apply([null]));
      },
      'break': function(cont, label, value) {
        var exitCont, exits;

        label = label[1];
        exits = this.exits[label];
        if (!exits || exits === []) {
          throw new Error(label);
        }
        exitCont = exits[exits.length - 1];
        return this.cont(value, this.protect(exitCont));
      },
      'continue': function(cont, label) {
        var continueCont, continues;

        label = label[1];
        continues = this.continues[label];
        if (!continues || continues === []) {
          throw new Error(label);
        }
        continueCont = continues[continues.length - 1];
        return this.protect(continueCont).call(null);
      },
      'catch': function() {
        var cont, forms, tag, v, v2;

        cont = arguments[0], tag = arguments[1], forms = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        v = il.vari('v');
        v2 = il.vari('v2');
        return this.cont(tag, il.clamda(v, il.pushCatch(v, cont), this.expsCont(forms, il.clamda(v2, il.popCatch(v), cont.call(v2)))));
      },
      "throw": function(cont, tag, form) {
        var v, v2;

        v = il.vari('v');
        v2 = il.vari('v2');
        return this.cont(tag, il.clamda(v, this.cont(form, il.clamda(v2, this.protect(il.findCatch(v)).call(v2)))));
      },
      'unwind-protect': function() {
        var cleanup, compiler, cont, form, oldprotect, result, v1, v2;

        cont = arguments[0], form = arguments[1], cleanup = 3 <= arguments.length ? __slice.call(arguments, 2) : [];
        oldprotect = this.protect;
        v1 = il.vari('v3');
        v2 = il.vari('v4');
        compiler = this;
        this.protect = function(cont) {
          return il.clamda(v1, compiler.expsCont(cleanup, il.clamda(v2, oldprotect(cont).call(v1))));
        };
        result = this.cont(form, il.clamda(v1, this.expsCont(cleanup, il.clamda(v2, cont.call(v1)))));
        this.protect = oldprotect;
        return result;
      },
      'callcc': function(cont, fun) {
        var v;

        v = il.vari('v');
        return this.cont(fun, il.clamda(v, cont.call(v.call(cont, cont))));
      },
      'logicvar': function(cont, name) {
        return cont.call(il.newLogicVar(name));
      },
      'dummy': function(cont, name) {
        return cont.call(il.newDummyVar(name));
      },
      'unify': function(cont, x, y) {
        var x1, y1;

        x1 = il.vari('x');
        y1 = il.vari('y');
        return this.cont(x, il.clamda(x1, this.cont(y, il.clamda(y1, il.if_(il.unify(x1, y1), cont.call(true), il.failcont.call(false))))));
      },
      'notunify': function(cont, x, y) {
        var x1, y1;

        x1 = il.vari('x');
        y1 = il.vari('y');
        return this.cont(x, il.clamda(x1, this.cont(y, il.clamda(y1, il.if_(il.unify(x, y), il.failcont.call(false), cont.call(true))))));
      },
      'is': function(cont, vari, exp) {
        var v;

        v = il.vari('v');
        return this.cont(exp, il.clamda(v, il.bind(vari, v), cont.call(true)));
      },
      'bind': function(cont, vari, term) {
        return il.begin(il.bind(vari, il.deref(term)), cont.call(true));
      },
      'getvalue': function(cont, term) {
        return cont.call(il.getvalue(this.interlang(term)));
      },
      'succeed': function(cont) {
        return cont.call(true);
      },
      'fail': function(cont) {
        return il.failcont.call(false);
      },
      'orp': function(cont, x, y) {
        var fc, state, trail, v;

        v = il.vari('v');
        trail = il.vari('trail');
        state = il.vari('state');
        fc = il.vari('fc');
        return il.begin(il.assign(trail, il.trail), il.settrail(il.newTrail), il.assign(state, il.state), il.assign(fc, il.failcont), il.setfailcont(il.clamda(v, il.undotrail(il.trail), il.settrail(trail), il.setstate(state), il.setfailcont(fc), this.cont(y, cont))), this.cont(x, cont));
      },
      'notp': function(cont, goal) {
        var fc, state, trail, v;

        v = il.vari('v');
        trail = il.vari('trail');
        state = il.vari('state');
        fc = il.vari('fc');
        return il.begin(il.assign(trail, il.trail), il.settrail(il.newTrail), il.assign(fc, il.failcont), il.assign(state, il.state), il.setfailcont(il.clamda(v, il.undotrail(il.trail), il.settrail(trail), il.setstate(state), il.setfailcont(fc), cont.call(v))), this.cont(goal, fc));
      },
      'repeat': function(cont) {
        return il.begin(il.setfailcont(cont), cont.call(null));
      },
      'cutable': function(cont, goal) {
        var cc, v;

        cc = il.vari('cutcont');
        v = il.vari('v');
        return il.begin(il.assign(cc, il.cutcont), il.assign(il.cutcont, il.failcont), this.cont(goal, il.clamda(v, il.assign(il.cutcont, cc), cont.call(v))));
      },
      'cut': function(cont) {
        return il.begin(il.setfailcont(il.cutcont), cont.call(null));
      },
      'findall': function(cont, goal) {
        var fc, v;

        fc = il.vari('fc');
        v = il.vari('v');
        return il.let_([fc, il.failcont], il.setfailcont(il.clamda(v, il.setfailcont(fc), cont.call(v))), this.cont(goal, il.failcont));
      },
      'parse': function(cont, exp, state) {
        var oldState, v;

        v = il.vari('v');
        oldState = il.vari('state');
        return this.cont(state, il.clamda(v, il.assign(oldState, il.state), il.setstate(v), this.cont(exp, il.clamda(v, il.setstate(oldState), cont.call(v)))));
      },
      'parsetext': function(cont, exp, text) {
        var oldState, v;

        v = il.vari('v');
        oldState = il.vari('state');
        return this.cont(text, il.clamda(v, il.assign(oldState, il.state), il.setstate(il.array(v, 0)), this.cont(exp, il.clamda(v, il.setstate(oldState), cont.call(v)))));
      },
      'setstate': function(cont, state) {
        var v;

        v = il.vari('v');
        return this.cont(state, il.clamda(v, il.setstate(v), cont.call(v)));
      },
      'settext': function(cont, text) {
        var v;

        v = il.vari('v');
        return this.cont(text, il.clamda(v, il.setstate(il.array(v, 0)), cont.call(v)));
      },
      'setpos': function(cont, pos) {
        var v;

        v = il.vari('v');
        return this.cont(pos, il.clamda(v, il.assign(il.index(il.state, 1), v), cont.call(v)));
      },
      'getstate': function(cont) {
        return cont.call(il.state);
      },
      'gettext': function(cont) {
        return cont.call(il.index(il.state, 0));
      },
      'getpos': function(cont) {
        return cont.call(il.index(il.state, 1));
      },
      'eoi': function(cont) {
        var data, pos;

        data = il.vari('data');
        pos = il.vari('pos');
        return il.begin(il.listassign(data, pos, il.state), il.if_(il.ge(pos, il.length(data)), cont.call(true), il.failcont.call(false)));
      },
      'boi': function(cont) {
        return il.if_(il.eq(il.index(il.state, 1), 0), cont.call(true), il.failcont.call(false));
      },
      'eol': function(cont) {
        var c, pos, text;

        text = il.vari('text');
        pos = il.vari('pos');
        c = il.vari('c');
        return il.begin(il.listassign(text, pos, il.state), il.if_(il.ge(pos, il.length(text)), cont.call(true), il.begin(il.assign(c, il.index(text, pos, 1)), il.if_(il.or_(il.eq(c, "\r"), il.eq(c, "\n")), cont.call(true), il.failcont.call(false)))));
      },
      'bol': function(cont) {
        var c, pos, text;

        text = il.vari('text');
        pos = il.vari('pos');
        c = il.vari('c');
        return il.begin(il.listassign(text, pos, il.state), il.if_(il.eq(pos, 0), cont.call(true), il.begin(il.assign(c, il.index(text, il.sub(pos, 1))), il.if_(il.or_(il.eq(c, "\r"), il.eq(c, "\n")), cont.call(true), il.failcont.call(false)))));
      },
      'step': function(cont, n) {
        var pos, text, v;

        v = il.vari('v');
        text = il.vari('text');
        pos = il.vari('pos');
        return this.cont(n, il.clamda(v, il.listassign(text, pos, il.state), il.addassign(pos, v), il.setstate(il.array(text, pos)), cont.call(pos)));
      },
      'lefttext': function(cont) {
        return cont.call(il.slice(il.index(il.state, 0), il.index(il.state, 1)));
      },
      'subtext': function(cont, length, start) {
        var length1, pos, start1, text;

        text = il.vari('text');
        pos = il.vari('pos');
        start1 = il.vari('start');
        length1 = il.vari('length');
        return il.begin(il.listassign(text, pos, il.state), il.assign(start1, il.if_(il.ne(start, null), start, pos)), il.assign(length1, il.if_(il.ne(length, null), length, il.length(text))), cont.call(il.slice(text, start1, il.add(start1, length1))));
      },
      'nextchar': function(cont) {
        var pos, text;

        text = il.vari('text');
        pos = il.vari('pos');
        return il.begin(il.listassign(text, pos, il.state), cont.call(il.index(text, pos)));
      },
      'follow': function(cont, item) {
        var state;

        state = il.vari('state');
        return il.let_([state, il.state], this.cont(item, il.clamda(v, il.setstate(state), cont.call(v))));
      },
      'notfollow': function(cont, item) {
        var fc, state;

        state = il.vari('state');
        fc = il.vari('fc');
        return il.let_([fc, il.failcont, state, il.state], il.setfailcont(cont), this.cont(item, il.clamda(v, il.setstate(state), fc.call(v))));
      },
      'may': function(cont, exp) {
        return il.begin(il.appendFailcont(cont), this.cont(exp, cont));
      },
      'lazymay': function(cont, exp) {
        var fc, v;

        fc = il.vari('fc');
        v = il.vari('v');
        return il.let_([fc, il.failcont], il.setfailcont(il.clamda(v, il.setfailcont(fc), this.cont(exp, cont))), cont.call(v));
      },
      'greedymay': function(cont, exp) {
        var fc, v;

        fc = il.vari('fc');
        v = il.vari('v');
        return il.let_([fc, il.failcont], il.setfailcont(il.clamda(v, il.setfailcont(fc), cont.call(v))), this.cont(exp, il.clamda(v, il.setfailcont(fc), cont.call(v))));
      },
      'any': function(cont, exp) {
        var anyCont, fc, state, trail, v;

        fc = il.vari('fc');
        trail = il.vari('trail');
        state = il.vari('state');
        anyCont = il.vari('anyCont');
        v = il.vari('v');
        return il.begin(il.assign(anyCont, il.clamda(v, il.let_([fc, il.failcont, trail, il.trail, state, il.state], il.settrail(il.newTrail), il.setfailcont(il.clamda(v, il.undotrail(il.trail), il.settrail(trail), il.setstate(state), il.setfailcont(fc), cont.call(v))), this.cont(exp, anyCont)))), anyCont.call(null));
      },
      'lazyany': function(cont, exp) {
        var anyCont, anyFcont, fc, trail, v;

        fc = il.vari('fc');
        trail = il.vari('trail');
        v = il.vari('v');
        anyCont = il.vari('anyCont');
        anyFcont = il.vari('anyFcont');
        return il.begin(il.assign(anyCont, il.clamda(v, il.assign(trail, il.trail), il.settrail(il.newTrail), il.setfailcont(anyFcont), cont.call(v))), il.assign(anyFcont, il.clamda(v, il.undotrail(il.trail), il.settrail(trail), il.setfailcont(fc), this.cont(exp, anyCont))), il.assign(fc, il.failcont), anyCont.call(v));
      },
      'greedyany': function(cont, exp) {
        var anyCont, fc, v;

        fc = il.vari('fc');
        anyCont = il.vari('anyCont');
        v = il.vari('v');
        return il.begin(il.assign(anyCont, il.clamda(v, this.cont(exp, anyCont))), il.assign(fc, il.failcont), il.setfailcont(il.clamda(v, il.setfailcont(fc), cont.call(v))), anyCont.call(null));
      },
      'xxxchar': function(cont, item) {
        var c, data, pos, v, x;

        data = il.vari('data');
        pos = il.vari('pos');
        x = il.vari('x');
        c = il.vari('c');
        v = il.vari('v');
        return this.cont(item, il.clamda(v, il.listassign(data, pos, il.state), il.if_(il.gt(pos, il.length(data)), il["return"](il.failcont.call(v))), il.assign(x, il.deref(v)), il.assign(c, il.index(data, pos)), il.iff(il["instanceof"](x, il.symbol('Var')), il.begin(il.bind(x, c), il.setstate(il.array(data, il.add(pos, 1))), cont.call(il.add(pos, 1))), il.eq(x, c), il.begin(il.setstate(il.array(data, il.add(pos, 1))), cont.call(il.add(pos, 1))), il.attr(il.symbol('_'), il.symbol('isString')).call(x), il.if_(il.eq(il.length(x), 1), il.failcont.call(v), il["throw"](il["new"](il.symbol('ExpressionError').call(x)))), il["throw"](il["new"](il.symbol('TypeError').call(x))))));
      },
      'spaces': function(cont, item) {
        return cont.call(il.spaces(il.solver));
      },
      'spaces0': function(cont, item) {
        return cont.call(il.spaces0(il.solver));
      }
    };

    Compiler = Compiler;

    for (name in il) {
      vop = il[name];
      try {
        instance = typeof vop === "function" ? vop() : void 0;
      } catch (_error) {
        e = _error;
        continue;
      }
      if (instance instanceof il.VirtualOperation && __indexOf.call(il.excludes, name) < 0) {
        (function(name, vop) {
          return Compiler.prototype.specials['vop_' + name] = function() {
            var args, compiler, cont, i, length, params, _i, _ref;

            cont = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
            compiler = this;
            length = args.length;
            params = (function() {
              var _i, _results;

              _results = [];
              for (i = _i = 0; 0 <= length ? _i < length : _i > length; i = 0 <= length ? ++_i : --_i) {
                _results.push(il.vari('a' + i));
              }
              return _results;
            })();
            cont = cont.call(vop.apply(null, params));
            for (i = _i = _ref = length - 1; _i >= 0; i = _i += -1) {
              cont = (function(i, cont) {
                return compiler.cont(args[i], il.clamda(params[i], cont));
              })(i, cont);
            }
            return cont;
          };
        })(name, vop);
      }
    }

    _ref = ['char', 'followChars', 'notFollowChars', 'charWhen', 'stringWhile', 'stringWhile0', 'number', 'literal', 'followLiteral', 'quoteString'];
    _fn = function(name, vop) {
      return Compiler.prototype.specials[name] = function(cont, item) {
        var compiler, v;

        compiler = this;
        v = il.vari('v');
        return compiler.cont(item, il.clamda(v, cont.call(il[name](il.solver, v))));
      };
    };
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      name = _ref[_i];
      _fn(name, vop);
    }

    Compiler.prototype.optimize = function(exp, env) {
      var expOptimize;

      expOptimize = exp != null ? exp.optimize : void 0;
      if (expOptimize) {
        return expOptimize.call(exp, env, this);
      } else {
        return exp;
      }
    };

    Compiler.prototype.toCode = function(exp) {
      var exptoCode;

      exptoCode = exp != null ? exp.toCode : void 0;
      if (exptoCode) {
        return exptoCode.call(exp, this);
      } else {
        if (exp === void 0) {
          return 'undefined';
        } else if (exp === null) {
          return 'null';
        } else if (_.isNumber(exp)) {
          return exp.toString();
        } else if (_.isString(exp)) {
          return JSON.stringify(exp);
        } else if (exp === true) {
          return "true";
        } else if (exp === false) {
          return "false";
        } else if (_.isArray(exp)) {
          return JSON.stringify(exp);
        } else if (typeof exp === 'function') {
          return exp.toString();
        } else if (_.isObject(exp)) {
          return JSON.stringify(exp);
        } else {
          return exp.toString();
        }
      }
    };

    Compiler.prototype.expsCont = function(exps, cont) {
      var length, v;

      length = exps.length;
      if (length === 0) {
        throw new exports.TypeError(exps);
      } else if (length === 1) {
        return this.cont(exps[0], cont);
      } else {
        v = il.vari('v');
        return this.cont(exps[0], il.clamda(v, this.expsCont(exps.slice(1), cont)));
      }
    };

    Compiler.prototype.quasiquote = function(exp, cont) {
      var head, i, length, quasilist, v, _j, _ref1;

      if (!_.isArray(exp)) {
        return cont.call(exp);
      }
      length = exp.length;
      if (length === 0) {
        return cont.call(exp);
      }
      head = exp[0];
      if (!_.isString(head)) {
        return cont.call(exp);
      }
      if (!this.specials.hasOwnProperty(head)) {
        return cont.call(exp);
      }
      head = exp[0];
      if (head === "unquote") {
        return this.cont(exp[1], cont);
      } else if (head === "unquote-slice") {
        return this.cont(exp[1], cont);
      } else if (head === "quote") {
        return cont.call(exp);
      } else if (head === "string") {
        return cont.call(exp);
      } else {
        quasilist = il.vari('quasilist');
        v = il.vari('v');
        cont = cont.call(quasilist);
        for (i = _j = _ref1 = exp.length - 1; _j >= 1; i = _j += -1) {
          e = exp[i];
          if (_.isArray(e) && e.length > 0 && e[0] === "unquote-slice") {
            cont = this.quasiquote(e, il.clamda(v, il.assign(quasilist, il.concat(quasilist, v)), cont));
          } else {
            cont = this.quasiquote(e, il.clamda(v, il.push(quasilist, v), cont));
          }
        }
        return il.begin(il.assign(quasilist, il.list(head)), cont);
      }
    };

    Compiler.prototype.substMacroArgs = function(exp, params) {
      var head, length;

      if (__indexOf.call(params, exp) >= 0) {
        return ['evalarg', exp];
      }
      if (!_.isArray(exp)) {
        return exp;
      }
      length = exp.length;
      if (length === 0) {
        return exp;
      }
      head = exp[0];
      if (!_.isString(head)) {
        return exp;
      }
      if (!this.specials.hasOwnProperty(head)) {
        return exp;
      }
      if (head === 'lambda' || head === 'macro') {
        params = ((function() {
          var _j, _len1, _results;

          if (__indexOf.call(exp[1], e) < 0) {
            _results = [];
            for (_j = 0, _len1 = params.length; _j < _len1; _j++) {
              e = params[_j];
              _results.push(e);
            }
            return _results;
          }
        })());
        return exp.slice(0, 2).concat((function() {
          var _j, _len1, _ref1, _results;

          _ref1 = exp.slice(2);
          _results = [];
          for (_j = 0, _len1 = _ref1.length; _j < _len1; _j++) {
            e = _ref1[_j];
            _results.push(this.substMacroArgs(e, params));
          }
          return _results;
        }).call(this));
      } else if (head === 'quote') {
        return exp;
      } else if (head === 'string') {
        return exp;
      } else if (head === 'quasiquote') {
        return exp;
      } else {
        return [exp[0]].concat((function() {
          var _j, _len1, _ref1, _results;

          _ref1 = exp.slice(1);
          _results = [];
          for (_j = 0, _len1 = _ref1.length; _j < _len1; _j++) {
            e = _ref1[_j];
            _results.push(this.substMacroArgs(e, params));
          }
          return _results;
        }).call(this));
      }
    };

    Compiler.prototype.interlang = function(term) {
      var head, length;

      if (_.isString(term)) {
        return il.vari(term);
      } else {
        return term;
      }
      if (!_.isArray(term)) {
        return term;
      }
      length = term.length;
      if (length === 0) {
        return term;
      }
      head = term[0];
      if (!_.isString(head)) {
        return term;
      }
      if (head === 'string') {
        return term[1];
      }
      return term;
    };

    return Compiler;

  })();

  augmentOperators = {
    add: il.addassign,
    sub: il.subassign,
    mul: il.mulassign,
    div: il.divassign,
    mod: il.modassign,
    'and': il.andassign,
    'or': il.orassign,
    bitand: il.bitandassign,
    bitor: il.bitorassign,
    bitxor: il.bitxorassign,
    lshift: il.lshiftassign,
    rshift: il.rshiftassign
  };

  exports.Env = Env = (function() {
    function Env(outer, data) {
      this.outer = outer;
      this.data = data != null ? data : {};
    }

    Env.prototype.extend = function(vari, value) {
      var data;

      data = {};
      data[vari.name] = value;
      return new Env(this, data);
    };

    Env.prototype.extendBindings = function(bindings) {
      return new Env(this, bindings);
    };

    Env.prototype.lookup = function(vari) {
      var data, name, outer;

      data = this.data;
      name = vari.name;
      if (data.hasOwnProperty(name)) {
        return data[name];
      } else {
        outer = this.outer;
        if (outer) {
          return outer.lookup(vari);
        } else {
          return vari;
        }
      }
    };

    return Env;

  })();

  exports.Error = Error = (function() {
    function Error(exp, message, stack) {
      this.exp = exp;
      this.message = message != null ? message : '';
      this.stack = stack != null ? stack : this;
    }

    Error.prototype.toString = function() {
      return "" + this.constructor.name + ": " + this.exp + " >>> " + this.message;
    };

    return Error;

  })();

  exports.TypeError = TypeError = (function(_super) {
    __extends(TypeError, _super);

    function TypeError() {
      _ref = TypeError.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    return TypeError;

  })(Error);

  exports.ArgumentError = ArgumentError = (function(_super) {
    __extends(ArgumentError, _super);

    function ArgumentError() {
      _ref1 = ArgumentError.__super__.constructor.apply(this, arguments);
      return _ref1;
    }

    return ArgumentError;

  })(Error);

  exports.ArityError = ArityError = (function(_super) {
    __extends(ArityError, _super);

    function ArityError() {
      _ref2 = ArityError.__super__.constructor.apply(this, arguments);
      return _ref2;
    }

    return ArityError;

  })(Error);

}).call(this);

/*
//@ sourceMappingURL=core.map
*/
