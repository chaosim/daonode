// Generated by CoffeeScript 1.6.2
(function() {
  var compile, compile_print, done, xexports;

  xexports = {};

  exports.Test = {
    "test 1": function(test) {
      var cont, f, x, xcont, ycont;

      x = null;
      cont = function(v) {
        return v;
      };
      ycont = function(v) {
        return (function(v) {
          return cont(x + v);
        })(2);
      };
      xcont = function(v) {
        x = v;
        return ycont(null);
      };
      f = function(v) {
        return xcont(1);
      };
      test.equal(f(null), 3);
      return test.done();
    }
  };

  done = function(exp) {
    return function(v) {
      return exp;
    };
  };

  compile = function(exp, cont) {
    return cont(exp);
  };

  compile_print = function(exp, cont) {
    var f;

    f = compile(null, cont);
    return function(exp0) {
      console.log(exp);
      return f(null);
    };
  };

  exports.Test = {
    "test 1": function(test) {
      var f, f2, x;

      x = 0;
      f = function(x) {
        var f1;

        return f1 = function() {
          return x;
        };
      };
      console.log(f(x).toString());
      f = compile(2, done);
      console.log(f.toString());
      test.equal(f(null), 2);
      f2 = compile_print(1, done);
      console.log(f2.toString());
      test.equal(f2(0), null);
      return test.done();
    }
  };

}).call(this);

/*
//@ sourceMappingURL=testExperiment.map
*/
